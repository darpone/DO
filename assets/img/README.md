# Image assets

New optimized image assets should be placed here. The original brand PNG files remain at the repository root to preserve their exact bytes and existing public URLs:

- `/delta-logo-exact.png`
- `/delta-triangle-tech.png`
- `/favicon.png`
